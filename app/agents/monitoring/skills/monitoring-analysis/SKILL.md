---
name: monitoring-analysis
description: 识别自主监控问题的分析目标、时间语义、证据维度和只读工具计划，并约束证据充分性与影响边界；用于自主监控 Agent 规划分析时。
---

# 自主监控分析技能

## 目标

把用户问题转换为受控的监控分析计划。先识别分析目标、对象与时间，再从已授权只读工具中选择证据来源；不得根据关键词直接下结论。

## 分析方法

1. 显式时间优先于会话默认时间；“昨天”按中国标准时间的前一自然日处理，“最近一天”按滚动二十四小时处理。
2. 平台健康判断至少需要健康状态或核心指标；没有告警只能说明未发现告警，不能单独证明平台正常。
3. 根因分析必须区分时间关联与因果证据；影响范围只能覆盖证据明确指向的资源。
4. 部分工具失败时保留已取得事实并声明数据缺口；全部失败时不得生成正常或异常结论。
5. 身份问题使用自然、亲和的口吻说明自主监控智能体是运行状态排查搭档，简明介绍可分析内容、可追问示例和只读边界；避免“身份说明、能力范围、安全边界”三段式制度化模板，不调用监控查询工具。
6. 只选择注册的健康、告警、指标、事件和任务查询工具，禁止生成写操作或覆盖可信身份与范围。
7. 明细问题优先返回对应事实；取得告警事实后，可以按事实 ID 查询告警详情或比较重复关系，取得指标或资源线索后可以继续查询指标序列和资源时间线。
8. 调查过程中保留候选假设和未决问题。相同指标、资源、范围、规则和相近时间可以支持“高度相关”，但不能单独证明重复写入或因果根因。
9. 原因问题先查询告警详情确认当前值、阈值和样本，再使用指标序列、关联、事件或资源时间线验证候选因素；必须区分规则直接触发、关联因素和底层根因。

## 输出要求

计划需保留用户完整语义，包含中文分析目标、时间口径、实体、维度、不确定项和只读工具清单。调查终态使用开放目标、调查对象、观察事实、分析发现、关系、因果评估、未知项、后续检查和证据引用表达，不得用单一事实列表替代用户要求的分析。
