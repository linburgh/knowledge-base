# AGENTS.md

## 项目定位

本项目是基于 FastAPI 与 LangChain 的企业知识库问答系统后端脚手架。

当前阶段只保留工程骨架，不落地具体 API、Service、DB、RAG、Worker 业务实现。

## 关联项目：ai-code-review

- 项目名称固定为 `ai-code-review`，定位为面向研发团队的智能代码审查 Agent。
- 项目接收 Pull Request、Merge Request、提交范围或本地补丁，结合代码差异、仓库上下文、静态分析工具和项目规则生成可定位、可验证的审查结果。
- 项目支持审查任务编排、问题确认与反馈、规则配置、代码平台评论回写、权限、审计、评测和部署等完整能力。
- `ai-code-review` 是独立项目；当前知识库仓库只维护其需求设计文档，不在本项目中直接实现该项目的 API、Service、DB、Agent 或 Worker。
- 相关设计文档统一放在 `docs/code-review/`，主文档为 [`ai-code-review-需求设计文档.md`](docs/code-review/ai-code-review-需求设计文档.md)。
- 后续实现必须遵守该设计文档的只读审查、安全沙箱、最小上下文、权限隔离、结果可追溯和人工确认原则，不得默认直接修改或发布代码。

## 关联项目：智能售前讲解员

- 项目名称为“智能售前讲解员”，定位为面向销售、售前顾问和解决方案架构师的客户 PPT 智能讲解与现场问答 Agent。
- 项目支持客户与商机上下文、产品 PPT 解析、页面级讲解稿、辅助讲解、AI 自动讲解、实时语音问答、人工接管、风险拦截、会议纪要和跟进任务闭环。
- 项目必须以已审核的产品资料为事实来源，价格、交付、定制、合规和竞品等高风险内容不得未经人工确认自动播报。
- “智能售前讲解员”是独立项目；当前知识库仓库只维护其需求设计文档，不在本项目中直接实现该项目的 API、Service、DB、Agent 或 Worker。
- 相关设计文档统一放在 `docs/pre-sales-assistant/`，内部需求主文档为 [`智能售前讲解员需求设计文档.md`](docs/pre-sales-assistant/智能售前讲解员需求设计文档.md)，客户交流文档为 [`智能售前讲解员解决方案.md`](docs/pre-sales-assistant/智能售前讲解员解决方案.md)。

## 关联项目：知识库问答自主评测

- 自主评测是知识库问答系统的独立评测模块，包含评测 Agent、平台管理任务、LangGraph 工作流、评测结果和原型设计。
- 自主评测相关设计文档必须统一放在 `docs/自主评测/` 下，后续新增的需求补充、原型、接口、数据表、工作流和实施文档不得放入通用 `docs/前端原型设计/` 或其他业务目录。
- 当前阶段先完成原型设计和评审，原型、需求和接口文档评审通过前，不得开始自主评测模块的前端、后端、数据库或 Agent 编码。
- 当前目录中的主要文档包括：`自主评测模块原型设计.md`、`知识库问答评测Agent需求与实施方案.md`、`知识库问答评估与调优方案.md` 和 `知识库问答评估与调优实施方案.md`。

## 知识库问答配置文档索引

- 知识库问答配置属于知识库工作区“问答”模块，配置入口为问答页面右侧滑块，不作为工作区独立 Tab。
- 相关需求、交互原型、后端配合改造方案和实施方案必须统一放在 `docs/知识库问答配置/` 下，不再分散到通用原型目录或 `docs/` 根目录。
- 当前文档索引：
  - [`知识库问答配置需求设计文档.md`](docs/知识库问答配置/知识库问答配置需求设计文档.md)：完整需求、页面入口、配置字段和交互规则。
  - [`知识库问答配置交互原型.html`](docs/知识库问答配置/知识库问答配置交互原型.html)：问答页面及右侧配置滑块的可交互原型。
  - [`知识库问答配置后端配合改造方案.md`](docs/知识库问答配置/知识库问答配置后端配合改造方案.md)：后端分层、数据、接口、权限和问答链路改造方案。
  - [`知识库问答配置前端开发实施方案.md`](docs/知识库问答配置/知识库问答配置前端开发实施方案.md)：问答页面、右侧配置滑块、表单交互、接口联调和前端验收。
  - [`知识库问答配置后端开发实施方案.md`](docs/知识库问答配置/知识库问答配置后端开发实施方案.md)：DDL、DB、Service、API、问答链路接入和后端验收。
- 当前阶段先完成原型和文档评审，评审通过前不得开始该配置模块的前后端业务编码。

## 目录边界

- `app/api/v1/`：HTTP 接口层，只负责请求解析、认证上下文、调用 Service 和响应转换。
- `app/core/services/`：业务编排层，负责校验、事务边界和调用 DB、RAG、外部服务。
- `app/db/`：数据访问层，负责连接、表结构、Repository 和向量库适配。
- `app/rag/`：LangChain 能力封装层，负责 Loader、Splitter、Embedding、Retriever 和 Chain。
- `app/schemas/`：请求、响应、分页和内部协议模型。
- `app/core/common/`：认证、异常、日志和纯工具函数。
- `workers/`：异步任务入口。
- `scripts/db/`：统一维护 DDL。
- `etc/`：配置样例和部署配置。

## Agent Harness 工程约束

`app/agents/` 下的每个 Agent 都必须是独立的 Harness 子工程。后续新增 Agent 时，必须新建独立目录，不得把新的 Agent 主入口、运行循环、工具或技能文件直接堆放在 `app/agents/` 根目录，也不得复用其他 Agent 的私有入口、Prompt、Runtime 或工具注册表。

标准目录结构以现有知识库问答 Agent Harness 为基础：

```text
app/agents/<agent_name>/
├── __init__.py
├── agent.py                 # 独立 Agent 入口和结构化调用协议
├── runtime.py               # 执行循环、超时、预算、重试和失败收敛
├── policies.py              # 权限、数据范围、工具白名单和安全策略
├── tools/
│   ├── __init__.py
│   ├── registry.py          # 显式工具注册
│   └── ...                  # 该 Agent 专属工具
└── skills/
    └── .../SKILL.md        # 该 Agent 专属技能指导
```

Agent 可以根据自身业务在独立目录内增加 `graph.py`、`state.py`、`config.py`、`models.py`、`dataset.py`、`generator.py`、`executor.py`、`metrics.py`、`report.py` 等领域模块，但不能因此省略 Harness 的入口、运行时、策略和工具边界。Agent 专属配置、状态、数据集和报告逻辑必须留在该 Agent 目录内；跨层请求、响应和内部协议统一放在 `app/schemas/`。

每个 Agent 必须满足以下约束：

- 具有独立入口，例如 `app/agents/knowledge/agent.py`、`app/agents/evaluation/agent.py`。
- 具有独立 `runtime.py`、`policies.py` 和 `tools/registry.py`，执行预算、权限策略和工具注册不可跨 Agent 隐式共享。
- 工具调用必须经过本 Agent 的显式注册、权限校验、数据范围校验和资源预算校验。
- Agent 之间只能通过公开的结构化协议调用，不得导入对方的私有 Prompt、私有函数、模型实例或内部状态。
- Agent 不负责 HTTP 请求解析、数据库事务编排或前端响应转换；调用方向保持为 `API -> Service -> Agent -> Tools / RAG / DB`。
- 不在 `app/agents/` 根目录新增通用主入口，不创建无法归属具体 Agent 的工具或技能。
- 未经明确设计，不新增 `contracts/`、`prompts/`、`guardrails/`、`tracing/`、`evals/` 等绕开既有 Harness 边界的目录；相关职责分别由 Schema、Agent、Runtime、Policies、工具注册和结构化日志承担。

自主评测 Agent 必须遵守该约束，目录固定为 `app/agents/evaluation/`，并与 `app/agents/knowledge/` 使用两个完整、独立的 Harness 结构。评测 Agent 的 LangGraph、状态、数据集、指标、报告和优化模块只能作为其 Harness 内的领域模块存在。

## 开发约定

- 遵守调用方向：`API -> Service -> DB / RAG / 外部服务`。
- 项目使用 `uv` 管理 Python、虚拟环境、依赖和锁文件。
- 新增依赖使用 `uv add`，不要手工编辑 `uv.lock`。
- `pyproject.toml` 是依赖声明源，`requirements.txt` 仅作为兼容导出文件。
- API 层不得直接访问数据库、向量库或 LLM。
- Service 层不得依赖 FastAPI 的 `Request`、`Response` 或 `HTTPException`。
- Service 层的新增、修改、删除等数据库写操作必须显式包裹在 `async with db.transaction():` 事务中；涉及多步数据库写入或读写组合时也必须使用同一事务，确保异常时能够完整回滚。纯查询方法无需额外开启事务。
- Service 层调用 DB 时优先调用对应表模块，例如 `app/db/document.py`；已有表模块时不要直接操作 `app/db/models.py` 或通用 `app/db/api.py`。
- DB 层不得拼接 Prompt、生成自然语言答案或处理 HTTP 展示文案。
- DB 层按表封装 `insert_`、`batch_insert`、`update_`、`delete_`、`get`、`list` 等通用方法，内部复用 `app/db/api.py`；过滤条件统一使用关键字参数传入。
- DB 层不要新增 `list_by_xxx`、`list_pending`、`delete_by_xxx`、`update_by_xxx` 这类只绑定单一字段或单一状态的方法。
- RAG 层不得决定用户权限，权限过滤应在业务检索流程中前置处理。
- 新增通用逻辑前先检查 `app/core/common/`、`app/db/api.py` 等已有公用方法，优先复用，避免重复实现。
- DB 层所有数据库操作必须经过统一数据库访问封装记录 SQL，至少记录操作类型、SQL 文本、执行结果和耗时；参数只记录参数名或脱敏摘要，禁止记录密码、Token、API Key、完整问题答案和其他敏感业务内容。
- 自主评测 Agent 必须记录从任务开始、配置校验、问题加载或生成、运行时调度、逐题执行与重试、知识库 Agent 调用、指标计算、报告持久化到成功、失败或取消结束的完整结构化日志；日志必须明确标识 `自主评测Agent` 或对应评测 Worker，能够证明执行经过 Agent Harness，不得只保留传统 Service/Worker 的状态更新日志。
- DDL 只维护在 `scripts/db/data_table_ddl.sql`。
- 数据库每张表必须包含名为 `id` 的自增主键，统一优先使用 `bigint generated by default as identity primary key`；业务字段不得替代表主键。
- 表结构可以保留 `xxx_id` 等关联字段，但 DDL 不得创建实际的 `foreign key` 外键约束；表之间的引用完整性、租户边界和删除策略由 Service / DB Repository 层保证。
- 数据库表设计和 DDL 禁止使用 `CHECK` 约束；跨字段、跨行和业务状态约束必须由 Service 事务校验、DB Repository 校验和必要的审计脚本共同保证。
- 唯一索引命名统一使用 `u_idx_<table>_<purpose>` 格式，不得为新增或调整的唯一索引使用 `uk_` 前缀；历史 `uk_` 索引在涉及变更时应同步迁移为 `u_idx_` 命名。
- DDL 中 `create index` 语句保持单行书写，例如 `create index if not exists idx_x on t_x (field);`。
- 密码、Token、API Key 和本机绝对路径不得提交到仓库。

## 方案设计约束

- 在用户没有明确要求缩小范围时，方案按完整目标设计和实现，不默认采用 MVP、最小可行版本、第一阶段裁剪或“先做简化版”的思路。
- 只有用户明确提出 MVP、分阶段交付、范围裁剪或简化实现时，才可以按这些限制设计方案。
- 方案文档应覆盖完整业务流程、页面/模块、接口、状态、异常、权限、扩展性和实施计划；不能擅自用 MVP 代替完整方案。
- 需求设计文档必须包含“功能模块”章节，明确一级模块、子模块、职责边界、输入输出和模块间关系。
- 需求设计文档各级章节标题应尽量使用四个字；为保证读者理解而使用缩写标题时，应在章节下增加“备注”说明完整含义，不得因追求四字标题删除必要语义。
- 不得在用户未提出或未确认的情况下扩展需求范围；监控、告警、报表、性能优化、运营能力和其他配套设计只能在用户明确要求或确认后写入方案。
- 对可能有价值但不属于当前范围的内容，只能作为待确认建议单独提出，不得直接纳入实施阶段、验收标准或交付物。

## 测试用例文档约束

- 每个新增模块、业务能力、Agent、接口、页面或重大交互设计，必须同步编写测试用例，不能只写需求、原型和实施方案。
- 测试用例统一放在 `docs/测试用例/` 下；不得散落在 `docs/` 根目录、通用原型目录或业务实施文档中。
- 前后端职责不同的模块，至少分别编写前端测试用例和后端测试用例；涉及 Agent、Worker、数据库、接口、权限或联调时，必须覆盖对应测试范围。
- 测试用例必须覆盖正常流程、边界条件、异常处理、权限校验、状态变化、数据一致性和验收标准；不能只列成功场景。
- 测试用例中的编号、前置条件、测试数据、操作步骤、预期结果和验收状态必须可执行、可追踪，并与需求、原型、接口和实施方案中的字段及状态保持一致。
- 需求或设计发生变更时，必须同步更新测试用例；测试用例未同步完成前，不得将对应能力标记为已完成或进入前后端联调验收。
- `docs/测试用例/` 应维护索引 README，新增测试用例文件必须登记到索引中。
- 测试执行过程中，凡是可以通过 Mock、故障注入、临时测试账号、临时测试数据、测试开关、独立进程或可控依赖替身自行构造的前置条件，必须由测试执行者自行搭建并继续测试，不得以“缺少真实故障环境”“没有测试账号”或“没有现成样例”作为停止、阻塞或不执行的理由；仅当涉及不可自行获得的新权限、外部系统授权或不可控生产资源时，才报告外部阻塞并说明已尝试的模拟方案。

## 前端项目约束

- 前端项目目录为 `/home/linburgh/workspace/ai-llm/knowledge-base-web`，与本后端项目同级。
- 前端项目名称固定为 `knowledge-base-web`。
- 前端技术栈固定为 Vue 3 + TypeScript + Vite + Element Plus（Vue 3 对应的 Element UI 组件库）+ Bootstrap + Vue Router + Pinia + Axios。交互组件使用 Element Plus，页面基础字体、排版、间距和布局遵循 Bootstrap。
- 原型和前端页面中的所有列表必须同时提供查询条件和分页，不得只实现静态列表。
- 列表操作列统一使用文本按钮，分页的上一页、下一页、页码和每页条数控件也统一使用文本按钮或文本样式，不使用带背景色的按钮。
- 列表的查询条件、分页和操作按钮属于统一 UI 设计原则，新增列表页面时必须同步设计并实现。

### 前端代码格式规范

- 前端源码统一使用项目已安装的 Prettier 和前端项目根目录的 `.prettierrc.json` 格式化，修改 `.vue`、`.ts`、`.css` 文件后必须保持格式化结果，不得手工保留压缩后的单行排版。
- 禁止将多个 HTML/Vue 元素、属性、插槽、事件绑定或条件表达式压缩在同一行；一个元素包含多个属性或子元素时必须分行书写。
- 禁止将多个 CSS 选择器、声明或媒体查询合并为难以阅读的单行规则；每个属性单独成行，相关样式按组件、页面和功能分组。
- 禁止将多个 TypeScript 变量声明、对象字段、函数调用、条件分支、`try/catch/finally` 或异步操作压缩在同一行；长表达式应按语义拆分。
- 禁止通过删除换行、缩短变量名或合并代码块来压缩源码体积；源码可读性优先，生产体积优化交给构建工具处理。
- 提交前必须执行前端项目的 `npm run format:check`，发现格式问题时执行 `npm run format` 后重新检查。
- 格式化只能调整排版，不得借格式化过程修改业务逻辑、接口参数、权限判断或数据处理行为；格式化产生的业务无关差异应一并检查。
- Vue 单文件组件必须保持可读的多行排版，禁止将多个变量声明、函数定义、条件分支或异步操作压缩在同一行。
- `<template>` 中的页面结构、表格列、表单项、按钮组和插槽内容应按层级缩进；一个元素包含多个子元素时必须拆分为多行，禁止使用大段单行模板。
- `<script setup lang="ts">` 中每个变量声明、函数和生命周期调用单独成行；函数体中的 `try/catch/finally`、条件判断和连续操作必须分层书写。
- 事件处理、数据加载、表单提交和删除确认等逻辑应使用具名函数，模板中只保留简短的方法调用，避免在模板中编写长表达式或多步语句。
- TypeScript 类型定义、对象字面量和组件 Props/Emits 定义应按字段或语义分行，避免使用难以阅读的超长单行声明。
- 表格列、查询条件和操作按钮必须保持统一缩进；查询条件字段、查询操作组和插槽内容应分别作为独立结构块维护。
- CSS 规则按组件或功能分组书写，禁止为了压缩体积将多组规则合并为超长单行；新增布局规则应优先复用公共组件和公共样式。
- 表单必须统一使用公共表单字体和间距规范：标签使用 13px、20px 行高，控件文字使用 13px、20px 行高，普通控件高度统一为 38px，抽屉表单项间距统一维护在公共样式中。
- 不得在单个页面中随意覆盖 Element Plus 表单标签、输入框、下拉框的字号、行高和控件高度；确有特殊场景时必须使用语义化的页面或组件类名，并同步说明适用范围。
- 表单标签、必填标记、输入内容和下拉选项必须保持同一字体体系和垂直对齐，禁止通过单独增加空格、特殊字符或单行压缩标签来实现排版。
- 修改前端代码后应执行 `npm run type-check` 和 `npm run build`；提交前检查是否存在明显的单行压缩代码和格式错位。
- 前端需求变更必须严格按“先修改原型稿，再修改设计/接口文档，最后修改前端代码”的顺序执行；原型稿和文档未同步完成前，不得开始对应的代码改造。
- 原型稿、设计文档和前端实现中的查询条件标签必须保持一致；查询条件标签统一使用四字名称，并且列表中必须存在语义对应的表格列。
- 项目中的角色、状态、权限和其他业务字典必须统一维护在前端公共字典模块中，页面、组件和详情展示只能复用公共字典选项或转换方法，不得在业务页面内重复定义编码与中文名称映射；新增字典项时同步补充类型、选项和转换方法，保证后续模块可复用。
- 所有列表中的创建时间、更新时间、加入时间等时间字段必须统一调用公共 `formatDateTime` 方法，展示格式固定为 `YYYY年MM月DD日 HH:mm:ss`，例如 `2026年08月09日 12:00:00`；禁止在页面内重复实现时间格式化。
- 列表字段内容过长时必须保持单行展示，不得自动换行；超出列宽的内容统一使用省略号，必要时通过 Element Plus 的 tooltip 提供完整内容。

## 前后端联调约束

- 前后端联调完成的页面必须使用真实后端接口，不得保留演示数据、占位数据、静态统计值或接口失败后的伪造兜底数据。
- 接口尚未提供时，可以先完成页面结构，但进入联调阶段必须移除对应占位数据；加载失败应展示错误状态，不得用本地数据替代接口响应。
- 图表数据必须来自统计接口，前端不得根据分页列表数据推算全量统计结果。
- 开发过程中发现后端接口缺失时，必须在对应模块的联调记录文档中登记接口缺口、建议路径、请求/响应范围和当前状态，并同步安排后端开发；不得静默跳过，也不得用前端演示数据替代。

## 后续落地顺序

1. 配置系统与启动入口。
2. 健康检查与统一异常。
3. 数据库连接和基础 DDL。
4. 知识库、文档、问答 Schema。
5. 文档入库 Service。
6. Loader、Splitter、Embedding、Retriever。
7. Chat Chain 与引用返回。
8. Worker、评测和部署。
