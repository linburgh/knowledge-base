# AGENTS.md

## 项目定位

本项目是基于 FastAPI 与 LangChain 的企业知识库问答系统后端脚手架。

当前阶段只保留工程骨架，不落地具体 API、Service、DB、RAG、Worker 业务实现。

## 目录边界

- `app/api/v1/`：HTTP 接口层，只负责请求解析、认证上下文、调用 Service 和响应转换。
- `app/core/services/`：业务编排层，负责校验、事务边界和调用 DB、RAG、外部服务。
- `app/db/`：数据访问层，负责连接、表结构、Repository 和向量库适配。
- `app/rag/`：LangChain 能力封装层，负责 Loader、Splitter、Embedding、Retriever 和 Chain。
- `app/schemas/`：请求、响应、分页和内部协议模型。
- `app/core/common/`：认证、异常、日志和纯工具函数。
- `workers/`：异步任务入口。
- `scripts/db/`：统一维护 DDL。
- `etc/`：配置样例和部署配置。

## 开发约定

- 遵守调用方向：`API -> Service -> DB / RAG / 外部服务`。
- 项目使用 `uv` 管理 Python、虚拟环境、依赖和锁文件。
- 新增依赖使用 `uv add`，不要手工编辑 `uv.lock`。
- `pyproject.toml` 是依赖声明源，`requirements.txt` 仅作为兼容导出文件。
- API 层不得直接访问数据库、向量库或 LLM。
- Service 层不得依赖 FastAPI 的 `Request`、`Response` 或 `HTTPException`。
- Service 层的新增、修改、删除等数据库写操作必须显式包裹在 `async with db.transaction():` 事务中；涉及多步数据库写入或读写组合时也必须使用同一事务，确保异常时能够完整回滚。纯查询方法无需额外开启事务。
- Service 层调用 DB 时优先调用对应表模块，例如 `app/db/document.py`；已有表模块时不要直接操作 `app/db/models.py` 或通用 `app/db/api.py`。
- DB 层不得拼接 Prompt、生成自然语言答案或处理 HTTP 展示文案。
- DB 层按表封装 `insert_`、`batch_insert`、`update_`、`delete_`、`get`、`list` 等通用方法，内部复用 `app/db/api.py`；过滤条件统一使用关键字参数传入。
- DB 层不要新增 `list_by_xxx`、`list_pending`、`delete_by_xxx`、`update_by_xxx` 这类只绑定单一字段或单一状态的方法。
- RAG 层不得决定用户权限，权限过滤应在业务检索流程中前置处理。
- 新增通用逻辑前先检查 `app/core/common/`、`app/db/api.py` 等已有公用方法，优先复用，避免重复实现。
- DDL 只维护在 `scripts/db/data_table_ddl.sql`。
- 数据库每张表必须包含名为 `id` 的自增主键，统一优先使用 `bigint generated by default as identity primary key`；业务字段不得替代表主键。
- 表结构可以保留 `xxx_id` 等关联字段，但 DDL 不得创建实际的 `foreign key` 外键约束；表之间的引用完整性、租户边界和删除策略由 Service / DB Repository 层保证。
- DDL 中 `create index` 语句保持单行书写，例如 `create index if not exists idx_x on t_x (field);`。
- 密码、Token、API Key 和本机绝对路径不得提交到仓库。

## 方案设计约束

- 在用户没有明确要求缩小范围时，方案按完整目标设计和实现，不默认采用 MVP、最小可行版本、第一阶段裁剪或“先做简化版”的思路。
- 只有用户明确提出 MVP、分阶段交付、范围裁剪或简化实现时，才可以按这些限制设计方案。
- 方案文档应覆盖完整业务流程、页面/模块、接口、状态、异常、权限、扩展性和实施计划；不能擅自用 MVP 代替完整方案。

## 前端项目约束

- 前端项目目录为 `/home/linburgh/workspace/ai-llm/knowledge-base-web`，与本后端项目同级。
- 前端项目名称固定为 `knowledge-base-web`。
- 前端技术栈固定为 Vue 3 + TypeScript + Vite + Element Plus（Vue 3 对应的 Element UI 组件库）+ Bootstrap + Vue Router + Pinia + Axios。交互组件使用 Element Plus，页面基础字体、排版、间距和布局遵循 Bootstrap。
- 原型和前端页面中的所有列表必须同时提供查询条件和分页，不得只实现静态列表。
- 列表操作列统一使用文本按钮，分页的上一页、下一页、页码和每页条数控件也统一使用文本按钮或文本样式，不使用带背景色的按钮。
- 列表的查询条件、分页和操作按钮属于统一 UI 设计原则，新增列表页面时必须同步设计并实现。

### 前端代码格式规范

- 前端源码统一使用项目已安装的 Prettier 和前端项目根目录的 `.prettierrc.json` 格式化，修改 `.vue`、`.ts`、`.css` 文件后必须保持格式化结果，不得手工保留压缩后的单行排版。
- 禁止将多个 HTML/Vue 元素、属性、插槽、事件绑定或条件表达式压缩在同一行；一个元素包含多个属性或子元素时必须分行书写。
- 禁止将多个 CSS 选择器、声明或媒体查询合并为难以阅读的单行规则；每个属性单独成行，相关样式按组件、页面和功能分组。
- 禁止将多个 TypeScript 变量声明、对象字段、函数调用、条件分支、`try/catch/finally` 或异步操作压缩在同一行；长表达式应按语义拆分。
- 禁止通过删除换行、缩短变量名或合并代码块来压缩源码体积；源码可读性优先，生产体积优化交给构建工具处理。
- 提交前必须执行前端项目的 `npm run format:check`，发现格式问题时执行 `npm run format` 后重新检查。
- 格式化只能调整排版，不得借格式化过程修改业务逻辑、接口参数、权限判断或数据处理行为；格式化产生的业务无关差异应一并检查。
- Vue 单文件组件必须保持可读的多行排版，禁止将多个变量声明、函数定义、条件分支或异步操作压缩在同一行。
- `<template>` 中的页面结构、表格列、表单项、按钮组和插槽内容应按层级缩进；一个元素包含多个子元素时必须拆分为多行，禁止使用大段单行模板。
- `<script setup lang="ts">` 中每个变量声明、函数和生命周期调用单独成行；函数体中的 `try/catch/finally`、条件判断和连续操作必须分层书写。
- 事件处理、数据加载、表单提交和删除确认等逻辑应使用具名函数，模板中只保留简短的方法调用，避免在模板中编写长表达式或多步语句。
- TypeScript 类型定义、对象字面量和组件 Props/Emits 定义应按字段或语义分行，避免使用难以阅读的超长单行声明。
- 表格列、查询条件和操作按钮必须保持统一缩进；查询条件字段、查询操作组和插槽内容应分别作为独立结构块维护。
- CSS 规则按组件或功能分组书写，禁止为了压缩体积将多组规则合并为超长单行；新增布局规则应优先复用公共组件和公共样式。
- 表单必须统一使用公共表单字体和间距规范：标签使用 13px、20px 行高，控件文字使用 13px、20px 行高，普通控件高度统一为 38px，抽屉表单项间距统一维护在公共样式中。
- 不得在单个页面中随意覆盖 Element Plus 表单标签、输入框、下拉框的字号、行高和控件高度；确有特殊场景时必须使用语义化的页面或组件类名，并同步说明适用范围。
- 表单标签、必填标记、输入内容和下拉选项必须保持同一字体体系和垂直对齐，禁止通过单独增加空格、特殊字符或单行压缩标签来实现排版。
- 修改前端代码后应执行 `npm run type-check` 和 `npm run build`；提交前检查是否存在明显的单行压缩代码和格式错位。
- 前端需求变更必须严格按“先修改原型稿，再修改设计/接口文档，最后修改前端代码”的顺序执行；原型稿和文档未同步完成前，不得开始对应的代码改造。
- 原型稿、设计文档和前端实现中的查询条件标签必须保持一致；查询条件标签统一使用四字名称，并且列表中必须存在语义对应的表格列。
- 项目中的角色、状态、权限和其他业务字典必须统一维护在前端公共字典模块中，页面、组件和详情展示只能复用公共字典选项或转换方法，不得在业务页面内重复定义编码与中文名称映射；新增字典项时同步补充类型、选项和转换方法，保证后续模块可复用。
- 所有列表中的创建时间、更新时间、加入时间等时间字段必须统一调用公共 `formatDateTime` 方法，展示格式固定为 `YYYY年MM月DD日 HH:mm:ss`，例如 `2026年08月09日 12:00:00`；禁止在页面内重复实现时间格式化。
- 列表字段内容过长时必须保持单行展示，不得自动换行；超出列宽的内容统一使用省略号，必要时通过 Element Plus 的 tooltip 提供完整内容。

## 前后端联调约束

- 前后端联调完成的页面必须使用真实后端接口，不得保留演示数据、占位数据、静态统计值或接口失败后的伪造兜底数据。
- 接口尚未提供时，可以先完成页面结构，但进入联调阶段必须移除对应占位数据；加载失败应展示错误状态，不得用本地数据替代接口响应。
- 图表数据必须来自统计接口，前端不得根据分页列表数据推算全量统计结果。

## 后续落地顺序

1. 配置系统与启动入口。
2. 健康检查与统一异常。
3. 数据库连接和基础 DDL。
4. 知识库、文档、问答 Schema。
5. 文档入库 Service。
6. Loader、Splitter、Embedding、Retriever。
7. Chat Chain 与引用返回。
8. Worker、评测和部署。
