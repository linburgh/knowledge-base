# 测试用例执行报告（2026-07-26）

## 1. 执行结论

本报告逐一核对了 `docs/测试用例/` 下 12 份测试文档，共识别 481 条表格用例。自动化可执行部分已在真实本地服务、PostgreSQL、Redis、MinIO、Ollama 和浏览器环境中执行；不能由当前自动化环境证明的手工、故障注入和完整业务矩阵用例，明确标记为“未执行”或“阻塞”，不以编译或冒烟结果替代。

## 2. 实际执行结果

| 执行项 | 结果 | 证据 |
| --- | --- | --- |
| 后端编译 | 通过 | `.venv/bin/python -m compileall -q app tests/integration workers` |
| 后端单元测试 | 通过，63 项 | `.venv/bin/python -m unittest discover -s tests -p 'test_*.py'`；新增 Embedding 受控并发测试 |
| 文档 API 冒烟与认证边界 | 通过，23 项 | `tests/integration/test_documented_api_smoke.py`；管理员、访客和未认证请求均按预期返回 |
| 平台用户、租户、组织 CRUD | 通过 | `tests/integration/test_platform_crud.py`；临时数据已软删除 |
| 平台边界与参数校验 | 通过，35 项 | `tests/integration/test_documented_boundaries.py`；授权 CRUD、跨租户边界、候选成员、租户依赖删除、重复编码、非法邮箱、必填字段、重复用户名、短密码、组织自父级、评测用户 ID 类型均通过，临时数据已清理 |
| 知识库 CRUD | 通过，6 个接口 | `tests/test_knowledge_base_crud.sh all`，使用真实管理员令牌 |
| 文档上传、索引、详情、列表 | 通过 | `tests/test_document_ingestion.sh all`；最小 Markdown 和 1MiB Markdown 均完成 pending → processing → ready |
| 文档索引 Worker 改造 | 通过基础验证 | 上传不再创建临时 `asyncio` 协程，服务启动后由持久化 Worker 接管；遗留任务可恢复，Embedding 请求出现 200 响应且任务持续刷新更新时间 |
| 文档安全与边界 | 通过，8 项 | `tests/integration/test_document_boundaries.py`；非法扩展名、空文件、拒绝上传无副作用、分片查询和不存在资源均通过 |
| 知识库问答 | 通过 | `tests/test_chat_flow.sh`，真实返回答案、引用和检索统计 |
| 会话、消息、引用 CRUD | 通过 | `tests/test_conversation_crud.sh all`，临时会话已逻辑删除 |
| 问答配置流程 | 通过，9 项 | `tests/integration/test_qa_config_flow.py`；默认配置、Prompt 预览、检索测试、Rerank 测试、非法配置、草稿读回、发布和恢复默认通过 |
| 知识库问答评测集 | 通过，20 条 | `tests/evals/runners/run_eval.py`；报告见 `tests/evals/reports/medical_signature-live.json` |
| 自主评测 Worker | 通过，1 个运行、1 条逐题结果 | `tests/integration/test_evaluation_worker.py`；运行报告结论为 `passed`，临时任务已逻辑删除 |
| 前端质量门禁 | 通过 | `npm run format:check`、`npm run type-check`、`npm run build` |
| 浏览器自动化 | 通过，9 项 | `npm run test:e2e`；覆盖管理页面、知识库工作区、问答配置五个页签、访客空问题/失败重试、查询分页、操作列、访客权限和接口失败错误态 |

重排联调说明：本地 `reranker-adapter` 仅用于开发阶段协议联调，不属于生产部署或生产验收范围。后端 Rerank 配置、失败策略、单项 Rerank 测试和问答链路仍按生产能力验收。

二次复测结果（2026-07-26 15:46 起）：后端单元测试 63 项通过，平台边界 35 项通过，文档边界 8 项通过，问答配置 9 项通过，自主评测 Worker 通过，前端 Playwright 9 项通过。切换阿里 Embedding 模型后，1MiB Markdown 文件生成 2,119 个分块，索引任务一次执行成功，约 41 秒进入 `ready`，临时知识库已删除。前端首次复跑因未注入测试账号环境变量导致 9 项启动失败，补充 `E2E_*` 账号变量后全部通过，该次不计为产品功能失败。此前对本地 `reranker-adapter` 的全量评测已从生产验收范围移除。

新增的可重复执行脚本均不写入固定账号或密码，运行时通过环境变量传入：

- `tests/integration/test_documented_boundaries.py`：35 项平台输入、授权、资源依赖与状态校验。
- `tests/integration/test_document_boundaries.py`：8 项文件上传、空文件、非法扩展名和分片查询边界。
- `tests/integration/test_qa_config_flow.py`：9 项问答配置生命周期、检索和 Rerank 配置校验。
- `knowledge-base-web/tests/e2e/documented-pages.spec.ts`：新增接口 500 错误态、问答配置页签和访客失败重试浏览器场景，Playwright 总计 9 项。

## 3. 逐文档覆盖状态

| 文档 | 已执行范围 | 未完成范围 | 状态 |
| --- | --- | --- | --- |
| 01-平台概览测试用例 | 平台概览真实接口、管理员页面路由、未认证和访客权限 | 空数据、延迟、500 注入、响应式图表逐项验收 | 部分执行 |
| 02-用户管理测试用例 | 用户分页接口、管理员 CRUD、重复用户名、非法邮箱、必填字段、短密码、页面加载/查询/分页/文本操作 | 重复邮箱、禁用当前用户、跨租户修改故障矩阵 | 部分执行 |
| 03-租户管理测试用例 | 租户分页、创建、修改、删除、重复编码、必填字段和含知识库删除约束，页面加载/查询/分页 | 成员资源约束的完整矩阵、停用租户后的完整访问链路 | 部分执行 |
| 04-组织管理测试用例 | 组织创建、修改、删除、必填字段、自父级校验、成员候选分页、成员添加/修改/删除、查询页面 | 循环层级、多成员批量回滚、完整角色矩阵 | 部分执行 |
| 05-开发者中心测试用例 | 开发者中心路由和页面加载 | 接口示例逐项比对、限流、模型异常追踪 | 部分执行 |
| 06-知识库列表测试用例 | 知识库 CRUD、分页、管理员权限、用户/组织授权 CRUD、重复授权、跨租户授权边界、列表页面查询分页 | 提示词超长和含文档/会话/索引知识库的删除回滚 | 部分执行 |
| 07-知识库概览测试用例 | 概览真实接口、知识库工作区路由 | 空库、趋势边界、统计故障注入 | 部分执行 |
| 08-文档管理测试用例 | 最小 Markdown、1MiB Markdown 上传并 ready、异步索引、详情、列表、非法扩展名、空文件、拒绝上传无副作用、分片查询 | 失败重试、删除回滚、前端分片分页 | 部分执行 |
| 09-知识库问答测试用例 | QA 配置查询、Prompt 预览、检索测试、Rerank 测试、非法配置、草稿发布恢复、真实问答、20 条生产评测集、引用和性能指标 | 五个配置 Tab 全量交互、生产重排异常注入和新旧会话对比 | 部分执行 |
| 10-独立问答测试用例 | guest 知识库列表、20 条 guest 评测、访客页面和平台越权 | 空知识库、输入边界、网络失败重试、租户切换完整链路 | 部分执行 |
| 自主评测前端测试用例 | 9 项 Playwright、表单参数、查询条件、权限、主要路由、接口失败错误态、问答配置页签、访客失败重试和构建 | 完整新增→执行→报告→优化→复测→草稿链路、空数据/轮询细节 | 部分执行 |
| 自主评测后端测试用例 | 单元测试、真实任务/PUT/运行冲突、错误用户 ID、真实 Worker、权限和报告 | 生成模式、并发、超时、降级、重启恢复、优化复测、事务故障注入 | 部分执行 |

## 4. 测试过程中修复的问题

1. 管理类 API 缺少路由级认证依赖，未认证请求曾可访问用户、租户和平台统计接口；已补充认证和平台超级管理员依赖，并通过未认证/访客边界测试。
2. 知识库创建未写入数据库必填的数字 `created_by`；已根据 `owner_id` 解析数据库用户并写入数字用户 ID。
3. Worker 读取任务内嵌问题内容时错误优先读取不存在的文件；已改为优先使用保存的问题内容，并兼容完整文件名后缀。
4. 知识库修改审计字段使用模块内异步 `list` 函数，产生未等待协程告警；已改为 `list(values.keys())`。
5. 知识库可授权用户和组织成员候选接口曾直接返回 `password_hash`；已改为显式选择安全字段，并通过真实接口校验响应不包含敏感字段。
6. 组织成员候选分页在 DDL 不建立外键的约束下使用隐式 JOIN，导致 HTTP 500；已补充显式 JOIN 条件并通过分页、添加、修改、删除联调。
7. 租户删除未检查活动成员、组织和知识库，可能留下活动知识库孤儿数据；已改为存在活动依赖时返回 HTTP 409，并通过临时租户探针验证删除前后数据一致性。
8. Rerank 请求在候选分块不足最终返回数时传递过大的 `top_n`，导致适配器 HTTP 400 和 fail-open；已将 `top_n` 限制为候选分块数量。
9. 文档上传未拒绝空文件；已增加空文件校验并验证拒绝上传不产生文档记录。
10. 用户服务未校验邮箱格式；已增加邮箱格式校验并通过非法邮箱边界测试。
11. 访客问答请求失败时输入内容会被清空；已恢复失败前输入，浏览器用例验证可重试。
12. 文档上传后使用临时 `asyncio.create_task`，服务停止会留下永久 `running/processing`；已改为启动时运行的索引 Worker，增加失联恢复、任务总超时、Embedding 受控并发和重试。

## 5. 环境阻塞与后续执行条件

- 完整故障注入、响应式视觉、接口延迟/500、事务中途异常和多角色矩阵需要专门测试控制能力或人工操作，当前没有可安全注入的测试开关。
- 1 MiB 大文件索引已完成专项复测：阿里 Embedding 模型下生成 2,119 个分块，约 41 秒进入 `ready`，任务一次成功。测试脚本默认轮询窗口保留 30 分钟，用于覆盖不同生产模型的耗时差异。
