# 测试账号初始化测试用例

## 测试范围

验证 `scripts/db/default_acc_dml.sql` 能够安全、完整且可重复地创建七个联调测试账号，以及对应的平台、租户和组织角色关系。

## 测试数据

| 账号 | 平台角色 | 租户关系 | 组织角色 |
|---|---|---|---|
| `linburgh` | `p_super_admin` | 两个租户的 `tenant_admin` | `org_admin` |
| `tenant-admin-acc` | — | 独立租户的 `tenant_admin` | — |
| `zhangfei` | — | 组合角色租户的 `tenant_admin` | `org_member` |
| `zhugeliang` | — | 组合角色租户的 `tenant_member` | `org_member` |
| `guest` | — | `tenant_guest` | `org_member` |
| `multi-tenant` | — | 两个租户的 `tenant_member` | — |
| `single-tenant` | — | 一个租户的 `tenant_member` | — |

密码只在执行时转换为哈希并通过 `psql` 变量注入，不在脚本或本文档中保存明文密码与固定哈希。

## 测试用例

| 编号 | 前置条件 | 操作步骤 | 预期结果 | 验收状态 |
|---|---|---|---|---|
| ACCOUNT-SEED-001 | 已执行基础 DDL | 注入七个密码哈希并执行初始化脚本 | 创建七个启用账号，密码哈希符合当前认证算法 | 通过 |
| ACCOUNT-SEED-002 | 初始化脚本已执行一次 | 使用相同参数再次执行脚本 | 不产生重复账号、租户、组织或成员关系 | 通过 |
| ACCOUNT-SEED-003 | 七个账号已创建 | 分别使用运行时测试密码调用登录接口 | 七个账号均登录成功并返回访问令牌 | 通过 |
| ACCOUNT-SEED-004 | 七个账号已登录 | 查询认证上下文 | 平台、租户、组织角色与测试数据表一致 | 通过 |
| ACCOUNT-SEED-005 | 未提供任一密码哈希变量 | 执行初始化脚本 | 脚本立即停止并提示缺少的变量，不写入部分数据 | 通过 |
| ACCOUNT-SEED-006 | 查看仓库脚本和文档 | 搜索明文密码和固定密码哈希 | 仓库不保存测试密码或可直接使用的固定哈希 | 通过 |
