# 自主监控智能体协同测试用例

## 1. 测试范围

验证自主监控智能体与知识库问答智能体、自主评测智能体及文档处理与索引链路之间的结构化遥测、事件采集、指标聚合、告警规则、受控探针、环境快照、结果可信度判断、权限隔离和循环调用防护。

## 2. 功能测试用例

| 编号 | 场景 | 前置条件 | 操作步骤 | 预期结果 | 验收状态 |
| --- | --- | --- | --- | --- | --- |
| MON-AGENT-001 | 问答运行遥测 | 问答智能体可执行 | 执行一次正常问答并查询监控事件 | 记录请求开始、检索、模型调用和完成事件，包含脱敏的运行 ID、耗时、命中数和引用数 | 待执行 |
| MON-AGENT-002 | 问答失败分类 | 模拟检索失败、模型超时和权限拒绝 | 分别执行问答并查看监控数据 | 失败原因分别结构化记录，不把不同失败合并成笼统“系统异常” | 待执行 |
| MON-AGENT-003 | 监控问答探针 | 已配置专用监控租户、知识库和固定问题 | 触发一次监控探针 | 探针使用 `purpose=monitor_probe`，只读执行，不创建普通会话，不污染业务问答统计 | 待执行 |
| MON-AGENT-004 | 探针失败边界 | 使专用探针知识库或问答链路失败 | 执行探针并观察业务状态 | 只标记探针范围异常，不直接判定全部租户问答不可用 | 待执行 |
| MON-AGENT-005 | 探针预算 | 配置探针超时、频率和并发限制 | 连续触发超过预算的探针 | 超出预算的调用被拒绝或延迟，不影响正常问答请求 | 待执行 |
| MON-AGENT-006 | 评测执行证据 | 创建一个评测任务 | 执行完整评测并查询监控事件时间线 | 能看到领取、运行、问题执行、指标计算、报告生成和持久化等阶段事件 | 待执行 |
| MON-AGENT-007 | 评测质量失败 | 评测执行完整但质量门禁不通过 | 查看评测结果和监控告警 | 评测标记 `quality_failed`，监控标记质量问题，不误报为 Worker 或基础设施故障 | 待执行 |
| MON-AGENT-008 | 评测环境异常 | 评测期间注入模型超时、向量库失败或 Worker 心跳过期 | 完成或中断评测并查看结果 | 评测与监控分别记录质量结果和环境异常，必要时标记 `environment_degraded` 或 `execution_failed` | 待执行 |
| MON-AGENT-009 | 评测结果不可判定 | 仅完成部分题目或有效样本不足 | 查询评测报告 | 结果标记 `inconclusive`，监控不得将其直接视为质量通过或质量失败 | 待执行 |
| MON-AGENT-010 | 评测环境快照 | 监控可提供环境快照 | 启动评测并在监控接口不可用时再次启动评测 | 正常时记录快照；监控不可用时评测仍可执行，并标记快照缺失 | 待执行 |
| MON-AGENT-011 | 评测作为质量探针 | 问答接口返回成功但引用率持续下降 | 完成一轮评测并观察监控 | 监控可关联评测质量结果生成告警，但不修改评测结论或自动调整配置 | 待执行 |
| MON-AGENT-012 | 告警关联 | 模拟索引失败、问答引用率下降和评测门禁不通过 | 查询事件关联结果 | 三类事件可合并到同一故障上下文，能够展示证据链和影响范围 | 待执行 |
| MON-AGENT-018 | 事件幂等 | 重复上报相同 `event_id` | 查询事件和聚合指标 | 重复事件只保留一份，不重复计数或重复触发告警 | 待执行 |
| MON-AGENT-019 | 状态快照 | 模拟 API、Worker、数据库和向量服务状态变化 | 查询监控健康状态 | 快照包含来源、采集时间和数据状态，能够区分健康、stale、unknown 和 error | 待执行 |
| MON-AGENT-020 | 指标聚合 | 产生不同租户、知识库和时间窗口的问答事件 | 查询趋势指标 | 按指定范围和窗口聚合，返回样本量、统计时间和计算状态，前端不自行推算 | 待执行 |
| MON-AGENT-021 | 无样本与采集失败 | 分别构造无样本、部分采集失败和完全采集失败 | 查看监控卡片和趋势 | 分别显示暂无数据、数据不完整和获取失败，不显示为 0 或健康 | 待执行 |
| MON-AGENT-022 | 规则最小样本数 | 仅产生少量问答失败，未达到最小样本数 | 等待多个统计周期 | 不触发高严重级别告警 | 待执行 |
| MON-AGENT-023 | 连续周期告警 | 让错误率连续达到配置阈值 | 查询告警实例 | 按连续周期规则触发一个告警实例，不重复创建告警 | 待执行 |
| MON-AGENT-024 | 告警恢复 | 先触发告警，再让指标连续满足恢复条件 | 查询告警生命周期 | 告警进入 resolved，记录恢复时间和恢复证据 | 待执行 |
| MON-AGENT-025 | 证据链 | 触发一条由多个事件和指标共同判断的告警 | 查看告警详情 | 告警可关联事件 ID、指标窗口、任务、Trace ID 或索引版本，不能只有自然语言结论 | 待执行 |
| MON-AGENT-026 | 规则与 Agent 分工 | 构造规则已触发但关联分析失败 | 查看告警状态和分析结果 | 告警仍由规则可靠生成，Agent 分析失败只影响摘要和建议，不丢失原始告警 | 待执行 |
| MON-AGENT-027 | 目标编码动态查询 | 通过系统发布或运维流程为同一 `target_code` 修改数据库采集配置 | 再次调用目标方法 | 后续调用读取新配置，不需要修改配置文件或重启应用；自主监控用户界面不提供该配置入口 | 待执行 |
| MON-AGENT-028 | 配置源与内存边界 | 通过系统发布或运维流程停用目标配置，并检查进程内存中的配置对象 | 调用目标方法并查询采集结果 | 数据库是唯一配置源；不加载全部配置，不长期缓存完整配置；无有效配置时跳过采集且不影响业务方法；用户不能在监控页面停用采集 | 待执行 |
| MON-AGENT-029 | 指标口径计算 | 构造成功、失败、超时、取消和降级的问答及任务样本 | 查询指标统计接口 | 成功率、错误率、超时率、降级率、任务成功率按需求文档公式计算，分母和排除项明确 | 待执行 |
| MON-AGENT-030 | 指标考核状态 | 分别构造达标、预警、不达标、无样本、样本不足和采集失败数据 | 查询指标卡片和告警 | 返回中文指标名、指标编码、样本数、阈值、考核状态和数据状态；无法判定不显示为达标 | 待执行 |
| MON-AGENT-031 | 百分位指标 | 构造足够和不足最小样本数的不同耗时数据 | 查询 P95、P99 和平均耗时 | 百分位使用原始完成样本计算，样本不足返回 `partial`，不得用平均值替代 | 待执行 |
| MON-AGENT-032 | 指标维度隔离 | 构造多个租户、知识库、接口、模型版本和任务类型数据 | 按不同维度查询指标 | 各维度数据不串租户、不串知识库，汇总值与明细可对账 | 待执行 |
| MON-AGENT-033 | 评测证据考核 | 构造仅更新运行状态和具备完整 Agent 生命周期事件两种运行 | 查询评测指标 | 仅有状态更新的运行证据完整率不达标；完整事件链才能计入 Agent 执行证明 | 待执行 |
| MON-AGENT-034 | 指标配置版本 | 修改数据库中的指标阈值并记录版本 | 在新旧窗口查询考核结果 | 新配置只影响生效时间后的统计和告警，历史结果保留原阈值版本及操作审计 | 待执行 |
| MON-AGENT-035 | 一级菜单位置 | 使用平台超级管理员和租户管理员请求菜单 | 查看左侧菜单和路由 | “自主监控”与“平台管理”同级，不出现在平台管理子菜单；默认路由为 `/monitoring/overview` | 待执行 |
| MON-AGENT-036 | 菜单权限隔离 | 分别使用平台超级管理员、租户管理员、普通角色和访客访问自主监控 | 查看菜单、路由、页面数据并执行告警操作 | 平台超级管理员可查看和操作授权平台范围；租户管理员可查看和操作当前租户范围；普通角色和访客菜单不可见且接口访问统一拒绝 | 待执行 |
| MON-AGENT-037 | 总览筛选联动 | 在总览切换平台/租户范围和 15 分钟、1 小时、24 小时等时间窗口 | 观察状态摘要、告警状态概览、资源容量图和链路状态 | 所有总览区域使用同一范围和时间窗口，查询条件可写入 URL，刷新后保持 | 待执行 |
| MON-AGENT-038 | 指标状态展示 | 构造完整、无样本、部分失败、过期和完全失败的统计结果 | 查看总览和指标分析页面 | 分别显示完整、暂无数据、数据不完整、数据过期和查询失败，不使用 0 或健康状态伪装 | 待执行 |
| MON-AGENT-039 | 告警证据展示 | 触发一条关联指标、事件、任务和 Trace ID 的告警 | 打开告警详情 | 展示中文指标名、指标编码、当前值、阈值、样本数、统计窗口和证据链；监控 Agent 分析失败不丢失原始告警 | 待执行 |
| MON-AGENT-040 | 数据表幂等 | 重复写入同一事件、指标时间桶和未恢复告警 | 查询数据表和页面 | 事件不重复计数，指标不重复聚合，同规则同范围同资源只保留一个未恢复告警 | 待执行 |
| MON-AGENT-041 | 数据敏感边界 | 在事件详情、指标详情和告警详情中查询问答及评测数据 | 检查页面展示和结构化字段 | 不展示完整问题、答案、文档片段、Token 或 API Key；仅展示脱敏摘要和关联 ID | 待执行 |
| MON-AGENT-042 | 分析会话复用 | 已有 `t_conversation`、`t_conversation_message` 和消息 `metadata` 模型 | 创建监控分析会话、发送追问、保存 Agent 回复并重新加载会话 | 不新增独立分析会话表、消息表或引用表；会话类型、监控范围、Agent 标识、上下文快照和证据引用可被正确保存和恢复 | 待执行 |
| MON-AGENT-043 | 分析 Agent 边界 | 同时存在自主监控 Agent 和自主评测 Agent 的运行数据 | 发起分析对话并检查调用链和持久化数据 | 分析对话由自主监控 Agent 处理；自主评测 Agent 只提供评测运行数据、质量结果和执行证据；历史对话归自主监控域管理 | 待执行 |
| MON-AGENT-044 | 阈值触发 | 构造达到最小样本数且超过阈值的指标窗口 | 等待规则周期计算并查询告警 | 规则引擎创建对应告警，记录当前值、阈值、样本数、规则版本和触发时间；自主监控 Agent 只负责后续分析 | 待执行 |
| MON-AGENT-045 | 连续触发 | 构造单周期超过阈值、连续三周期超过阈值两组数据 | 分别查询告警实例 | 单周期不满足连续规则不触发；连续满足后触发一条告警，不重复创建 | 待执行 |
| MON-AGENT-046 | 数据状态触发 | 构造 Worker 心跳过期、采集失败和无样本场景 | 等待状态规则计算 | 过期或配置的无数据条件可触发对应告警；无样本和采集失败不会被当作普通数值参与阈值计算 | 待执行 |
| MON-AGENT-047 | 告警恢复 | 已存在 firing 告警，连续满足恢复条件 | 查询告警生命周期和恢复证据 | 同一告警更新为 resolved，记录恢复时间、恢复值和恢复证据，不创建新的恢复告警 | 待执行 |
| MON-AGENT-048 | Agent 触发边界 | 构造规则未触发但 Agent 分析认为存在风险的场景 | 查询告警和分析结果 | Agent 不能直接创建告警；只有规则引擎满足条件后才能生成告警实例 | 待执行 |
| MON-AGENT-049 | 告警全流程 | 上报事件，运行 5 分钟指标聚合，配置阈值规则和通知策略，执行告警确认、恢复、通知发送失败与重试，再发起分析对话 | 按“事件采集 → 指标聚合 → 规则判断 → 告警生成/去重 → 告警确认 → 告警恢复 → 通知记录/失败重试 → 审计 → Agent 分析”顺序执行自动化测试 | 事件只计数一次；指标窗口可追溯；同一规则和范围只生成一个未恢复告警；确认和恢复更新同一告警；通知记录分别覆盖 pending、sent、failed 和最多 3 次重试；每个关键状态均有审计记录；分析回答由自主监控 Agent 生成并携带证据 | 已自动化通过 |
| MON-AGENT-050 | 身份介绍 | 分析上下文存在或不存在告警与证据 | 输入“你是谁？介绍一下自己” | 自主监控 Agent 使用自然、亲和的口吻说明自己是运行状态排查搭档，说明可分析内容、可直接追问的问题和只读安全边界；不使用“身份说明、能力范围、安全边界”三段式生硬模板，不返回无关兜底文案或附带无关证据 | 通过（后端自动化 + Chromium 自动化） |
| MON-AGENT-051 | 自然时间 | 会话默认范围为最近 1 小时，当前时区为 Asia/Shanghai | 输入“昨天平台运行正常吗”和明确日期问题 | 显式时间优先于会话默认值；昨天解析为前一自然日 00:00 至当日 00:00；消息元数据保存起止时间、时区、标签和来源 | 通过（后端自动化） |
| MON-AGENT-052 | 多源健康 | 指定窗口具备正常健康快照、核心指标且无异常告警、事件和任务 | 询问平台是否正常 | Agent 在预算内调用健康、告警、指标、事件和任务五类只读工具，返回 normal、完整数据状态、实际时间和证据 | 通过（后端自动化） |
| MON-AGENT-053 | 空证据判断 | 指定窗口五类工具均成功但无任何数据 | 询问平台是否正常 | 返回 unknown 和证据不足限制，不把“没有未恢复告警”表达为平台正常，不返回旧固定兜底文案 | 通过（后端自动化） |
| MON-AGENT-054 | 恢复告警 | 指定窗口存在已恢复或已关闭告警，健康快照和核心指标正常 | 询问平台是否正常 | 已恢复告警纳入窗口事实，结论至少为 warning，回答给出未恢复与已恢复告警数量 | 通过（后端自动化） |
| MON-AGENT-055 | 语义规划 | 配置可返回 Structured Output 的 Chat 模型 | 分别输入“昨晚平台是不是抖了一下”“发布之后服务稳不稳”“用户反馈很慢，系统侧有异常吗” | 不依赖固定关键词命中；模型输出目标、时间、实体、维度和只读工具计划，计划通过 Schema 和白名单校验后执行 | 通过（真实模型 + 后端自动化） |
| MON-AGENT-056 | 规划降级 | 模拟模型未配置、超时、非法结构和调用异常 | 输入需要分析的自然语言问题 | 自动使用有限规则计划；结果记录 `planning.mode=fallback`、失败类型和能力限制，不调用未授权工具 | 通过（后端自动化） |
| MON-AGENT-057 | 非法工具 | 模型计划返回写操作、未知工具或可信租户覆盖参数 | 执行计划校验 | 删除未授权工具并记录限制；没有合法工具时补齐只读安全计划；租户、用户和角色只能来自可信上下文 | 通过（后端自动化） |
| MON-AGENT-058 | 开放语义 | 模型返回未登记的新意图但提供合法分析目标和工具 | 执行结构化计划 | 新意图归一为 general_analysis，保留目标、实体、维度和歧义，不因封闭枚举导致规划失败 | 通过（后端自动化） |
| MON-AGENT-059 | 中文回答 | 面向中国客户发起身份、影响范围、开放分析和建议类问题，证据包含英文资源编码 | 查看规划结果、最终回答和分析页面提示 | 除接口编码及必要技术专名外，目标、不确定项、身份介绍、结论、限制、建议和页面提示均使用中文；回答不得直接展示英文结论、资源编码、Agent、Trace 或 Asia/Shanghai | 通过（后端自动化 + 前端检查） |
| MON-AGENT-060 | 基础排版 | 模拟回答模型不可用，并分别发起常规分析和身份问题 | 查看接口降级回答和浏览器消息气泡 | 常规降级回答按“分析结论、分析依据、判断边界、处理建议”组织；身份回答使用自然开场、简明能力说明、可追问示例和一句式只读边界，不使用制度化三段标题；浏览器安全渲染标题、强调和列表，不显示原始 Markdown 标记，用户消息仍按纯文本展示 | 通过（后端自动化 + Chromium 自动化） |
| MON-AGENT-061 | 动态排版 | 构造正常、异常、证据不足和原因分析事实，并模拟回答模型成功、超时和非法输出 | 查看接口回答与浏览器展示 | 模型在确定结论和授权证据范围内动态生成中文 Markdown，可按需使用引用块、列表、编号步骤和表格；不得改变结论或引入证据外事实；失败时返回确定性 Markdown；客户可见起止时间统一标注“中国标准时间” | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-044 | 异步发送 | 延迟分析消息接口，分别测试成功、失败和等待期间输入新草稿 | 提交问题并观察输入框、消息区和等待状态 | 提交后输入框立即清空、用户消息立即显示并出现分析中状态；发送期间不重复提交；成功后刷新正式消息；失败时仅在输入框为空时恢复原问题，不覆盖等待期间的新草稿 | 通过（Chromium 自动化） |
| MON-UI-048 | 证据抽屉 | 分析会话包含多轮回答，每轮关联不同数量和类型的证据 | 查看双栏布局，点击回答下方证据按钮，展开筛选面板，再选择类型、等级，执行重置、分页、切换每页条数、展开详情和关闭抽屉 | 页面不常驻参考资料区域；按钮显示当前回答证据数；抽屉只加载当前回答证据；筛选区按证据类型和证据等级分为两行并平铺固定单选项，点击立即筛选，不出现证据内容条件、下拉框和查询按钮；窄屏选项自然换行；列表支持后端分页、文本式分页控件和行内详情；覆盖加载、空数据、失败和无权限状态 | 部分通过（Chromium 自动化；独立后端分页及异常态待联调） |
| MON-AGENT-062 | 输出合同 | 模型分别返回表格、引用块、编号步骤和不同章节名称，并使用合法或非法证据引用 | 执行回答编排 | 合法排版均通过，不要求固定章节或逐字复述结论；结论确认编码必须一致，证据引用必须属于本轮授权事实；布局原因只进入元数据 | 通过（后端自动化） |
| MON-AGENT-063 | 自动修复 | 首次模拟缺少中国标准时间、结论编码不一致或结构不完整，第二次返回合法结果 | 执行回答编排 | 在同一超时预算内携带具体错误自动修复一次；修复成功使用模型回答，连续失败才降级 | 通过（后端自动化） |
| MON-AGENT-064 | 动态降级 | 模拟模型不可用，并分别提供多维健康数据、原因证据、少量事实和数据缺口 | 查看降级回答 | 多维健康数据使用表格，原因证据使用链路或编号，少量事实保持简洁，数据缺口突出覆盖范围；不统一输出固定四段式 | 通过（后端自动化） |
| MON-UI-001 | 导航层级 | 平台超级管理员或租户管理员具备自主监控查看权限 | 进入自主监控并切换左侧二级菜单 | 左侧展示平台管理、知识库管理、自主监控一级菜单及自主监控二级菜单；任务、告警、事件等三级内容在右侧 TAB 展示；总览和指标分析保持单页面 | 待执行 |
| MON-UI-002 | 告警聚合 | 当前范围存在多条不同级别的未恢复告警 | 进入监控总览并查看顶部告警区域 | 告警区域位于标题和筛选区域之后、健康摘要和图表之前；按严重、警告、提示聚合显示数量，不平铺告警详情，并提供告警中心查看入口 | 待执行 |
| MON-UI-003 | 高内聚总览 | 总览接口返回完整统计数据 | 进入监控总览并查看首屏 | 运行状态只展示平台服务、外部依赖和监控采集；业务状态只展示问答、异步任务和自主评测；告警状态展示四类状态数字及最近 3 条变化，页面同时展示资源容量和链路状态；完整明细通过入口进入对应模块 | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-054 | 告警概览 | 构造触发中、已确认、已恢复和已关闭告警，并准备超过 3 条状态变化 | 查看监控总览告警状态 | 四类状态数字来自总览统计接口；最近变化固定为告警信息、当前状态、最近变化三列并最多展示 3 条；列表按最近状态时间倒序，不从分页列表推算统计结果 | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-004 | 管理边界 | 平台超级管理员、租户管理员和其他角色进入数据采集、告警规则或通知模块 | 检查各模块操作入口并尝试进入数据采集详情 | 数据采集仅提供查询、“目标详情”和事件追溯；目标详情只展示运行事实和异常证据，不显示配置编辑项、版本管理、新增、编辑、启停、立即采集或删除入口；告警规则和通知按设计提供管理操作；其他角色不可见且不可访问 | 待执行 |
| MON-UI-005 | 抽屉可读 | 打开采集、告警、事件、任务或配置详情抽屉 | 检查标题、字段名、字段值、摘要和按钮 | 抽屉使用白底深色文字，内容清晰可读，不继承左侧导航浅色文字样式 | 待执行 |
| MON-UI-006 | 管理边界 | 用户进入指标、任务、事件、智能分析和审计页面 | 查看页面操作区 | 这些页面以查询、详情、证据和审计追溯为主，不出现未设计的业务删除、重试、暂停或终止操作 | 待执行 |
| MON-UI-007 | 指标中文名 | 进入指标分析并展开指标名称下拉框 | 查看全部指标选项 | 选择控件只展示中文指标名；指标编码仅在指标信息区、详情和接口语义中展示 | 待执行 |
| MON-UI-008 | 总览统一风格 | 进入监控总览并切换到数据采集、指标分析等页面 | 对比总览与其他页面视觉样式 | 总览与其他页面统一使用浅色管理后台风格，首页只保留告警、资源、链路状态和状态摘要，不使用深色大屏视觉 | 待执行 |
| MON-UI-009 | 业务入口下沉 | 构造问答错误、任务积压、索引待处理和评测运行事件 | 查看总览业务状态并悬停各状态块 Tip，再点击对应入口 | 总览只显示当前状态和关键值；每个状态块 Tip 解释其主要表达内容；问答趋势、任务明细和评测阶段进入对应模块，跳转携带范围、时间窗口和上下文 | 待执行 |
| MON-UI-010 | 链路状态展示 | 分别构造正常服务快照、Worker 心跳和业务事件，以及告警、异常任务、评测运行和 Trace ID | 查看链路状态 | 参考“参考证据”固定为链路信息、最近变化、更新时间三列，展示平台、任务、问答、评测和告警五行；名称与状态合并展示，事件摘要与记录信息合并展示；正常状态不留白，单条链路无记录时明确提示，不补造演示数据 | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-011 | 采集总览 | 构造正常、等待触发、部分失败、获取失败、数据过期和暂无数据的采集目标，并覆盖问答、索引、评测和外部依赖采集域 | 进入数据采集页面并查看首屏，检查状态趋势、执行结果和采集概览图例，再分别悬停状态图形 | 页面先展示整体采集结论、执行成功率、状态趋势和“执行成功率 + 状态占比带”；三处图例统一显示 `正常、待采、部分、失败、过期、暂无` 双字名称并独立布局，悬停时显示完整状态与数量；再展示 5 分钟粒度采集域热力图及状态 Tip，最后提供按目标名称查询、可分页的采集目标列表；查询、明细和详情仍展示完整业务状态；各区块标题均为四字名称，信息层级清晰且图表、文字和列表口径一致 | 通过（Chromium 自动化） |
| MON-UI-012 | 指标总览 | 构造达标、预警、不达标、暂无数据和数据过期指标 | 进入指标分析并查看首屏、查询栏和指标详情，分别悬停域状态条 | 页面沿用数据采集的浅色总览和统一查询栏样式；展示指标结论、5 分钟状态趋势、监控域状态图和指标明细；域状态图按行展示指标总数、健康率和最近更新时间，图例只展示状态名称并独立位于右上方，悬停状态条显示各状态数量；区块标题为指标结论、状态趋势、域状态图、指标明细；无样本、采集失败和过期指标不显示为达标；指标列表支持查询分页且不展示稳定指标编码 | 通过（Chromium 自动化） |
| MON-UI-013 | 任务总览 | 构造跨时间桶的待处理、新增、完成、失败、超时和取消任务，以及空闲、忙碌和心跳延迟 Worker；另构造完全无任务的时间范围 | 进入任务监控并查看首屏、查询栏和任务详情 | 页面沿用数据采集的浅色总览和统一查询栏样式；展示任务结论、5 分钟积压吞吐组合图、Worker 状态和任务明细；待处理积压为桶末存量阶梯线，新增、完成、失败和超时为桶内流量柱状图，图例依次使用“积压、新增、完成、失败、超时”五个双字名称，时间桶连续且取消不计入失败；区块标题为任务结论、状态趋势、工作状态、任务明细；无任务数据时仍展示 ECharts 坐标系、五项图例和图内空数据提示，不补造业务趋势；突出待处理数量和最老等待时长；空闲 Worker 不标记为异常；Worker 可作为筛选条件但不作为任务明细列表列展示；列表支持查询分页且不提供重试、暂停或终止操作 | 通过（后端聚合单测 + Chromium 自动化） |
| MON-UI-014 | 告警总览 | 构造触发中、已确认、已恢复和已关闭告警，并覆盖严重、警告和提示级别 | 进入告警中心查看首屏、趋势、优先分布、明细和详情操作 | 页面展示告警结论、告警趋势、优先分布和告警明细；突出未恢复、待确认、严重告警和最长持续时间；趋势区分新触发、未恢复和已恢复；优先分布只统计未恢复告警；告警表格展示首次发生、最近发生、确认人等字段；不同状态按权限和状态显示确认、抑制、关闭、备注或只读操作 | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-016 | 告警规则配置 | 构造启用、停用和多个版本的告警规则 | 在告警中心切换“规则配置”，查询规则并打开新增、编辑和查看操作 | 告警中心提供告警实例和规则配置二级视图；规则列表展示规则名称、监控域、级别、触发表达式、触发窗口、恢复条件、状态、版本和更新时间；“新增”位于查询条件栏操作组，区块标题旁不显示“新增规则”；操作列固定在右侧且“查看 / 编辑”“启用 / 停用”“审计”三个短标签单行完整展示，不出现“启用规则”“停用规则”“查看审计”；保存规则生成新版本，停用规则不删除历史告警实例；其他角色不可操作 | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-017 | 告警通知 | 构造站内通知和 Webhook 渠道，覆盖触发、持续、恢复和发送失败记录 | 在告警中心切换“告警通知”，查看渠道配置、通知策略和发送记录，并打开新增表单 | 告警通知作为告警中心内的第三个 Tab，不出现在一级导航；展示通知结论、渠道配置、通知策略和发送记录；渠道配置和通知策略分别支持快速查找与简单分页，两个区域卡片行高和分页底线对齐；发送记录支持查询分页；新增渠道和新增策略表单采用单列纵向布局、标签置于控件上方、40px 控件和底部右对齐按钮；通知失败只更新通知状态和失败原因，不影响告警实例保存；普通空轮询和 Worker 空闲不发送业务告警通知 | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-018 | 智能分析 | 构造告警、指标、事件、任务、评测运行和 Trace ID 的关联异常 | 通过侧栏进入智能分析 | 路由直接展示现有对话工作区；页面不显示“分析总览”和“分析对话”二级菜单；欢迎提示、当前异常范围、历史会话、快捷问题和追问输入均正常展示 | 通过（Chromium 自动化） |
| MON-UI-019 | 智能分析对话 | 构造预警状态的分析上下文，并提供告警、指标、任务和 Trace 关联证据 | 进入智能分析并新建会话，查看会话列表和消息区，切换历史会话，点击快捷问题、输入追问并发送 | 页面采用与知识库问答一致的双栏工作区；新会话名称为“新建分析会话”，不使用平台运行结论；支持新建、重命名与切换历史会话；回答绑定当前异常范围并显示证据引用；支持快捷问题、输入框和回车发送；不提供自动修改配置、重试任务或执行处置能力 | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-021 | 分析历史 | 已存在自主监控分析会话、消息和背景证据 | 进入智能分析，切换历史会话并继续追问，再刷新页面重新进入 | 历史会话复用通用会话和消息模型恢复；消息元数据保留自主监控 Agent、上下文快照和证据引用；刷新后不丢失历史；自主评测 Agent 的运行记录只作为引用证据，不承担对话处理 | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-022 | 侧栏视觉 | 已登录且自主监控菜单可见 | 进入数据采集页面，检查一级和二级菜单的默认、悬停及选中状态 | 一级菜单展示语义图标；当前一级菜单使用圆角深蓝底色；当前二级菜单使用左侧高亮蓝线、整行深蓝底色和统一缩进；文字清晰且非选中菜单仍可辨识 | 通过 |
| MON-UI-023 | 列表控件 | 自主监控数据采集、指标分析、任务监控、告警中心、事件中心和审计管理可访问 | 逐页检查查询区、列表、空列表和分页，并执行查询、重置、切页和修改每页条数 | 查询区使用 Element Plus 表单、输入、选择和按钮；列表使用 `el-table` 和自动表格布局；除展开控制列外，业务列不设置固定宽度或最小宽度；“操作入口”列保留明确宽度并固定在右侧；有数据和空数据时均保留 `el-pagination`；不存在原生业务表单、自绘表格、自绘页码或无实际绑定的占位查询控件 | 通过（Chromium 自动化） |
| MON-UI-055 | 简约列表 | 数据采集、指标分析、任务监控、告警中心和事件中心存在可查询数据 | 逐页检查采集目标、指标、任务、告警实例、规则、通知发送和事件列表，并打开详情 | 每个主列表最多四列，按业务采用“信息 / 状态或结果 / 时间 / 操作”组合结构；次要属性在单元格内分层展示，完整属性可从详情或编辑抽屉查看；查询条件、分页、文本操作按钮和长文本 Tooltip 保持可用 | 通过（Chromium 自动化） |
| MON-UI-024 | 总览留白 | 监控总览返回正常、空数据或异常数据 | 检查各模块高度、内边距、区块间距及状态切换 | 普通卡片内边距为 24–26px、区块间距为 20–22px；运行和告警图表约 280px，业务项不低于 92px，容量卡不低于 124px，链路状态区域保持 280px 最小高度；正常数据形成可感知的状态行，页面状态切换无明显跳动 | 通过（Chromium 自动化） |
| MON-UI-020 | 字段规范 | 进入数据采集、指标分析、任务监控、告警中心、事件中心、审计管理和告警通知中的所有查询列表 | 逐页检查查询条件标签、表格表头和操作列 | 所有查询条件字段和列表列名均为四字名称；不足四字的字段分别补充名称、状态、结果、入口或范围等限定词；不出现“操作”“状态”“资源”“Worker”等单字、两字或英文裸字段 | 待执行 |
| MON-UI-015 | 事件总览 | 构造任务、Worker、告警、采集和依赖来源事件，并包含正常与异常事件 | 进入事件中心查看首屏、趋势、来源分布、查询栏和事件详情 | 页面沿用“任务结论”的紧凑横向样式；重要事件展示最高优先级未恢复异常的事件内容、影响对象、状态、时间、关联情况和详情入口；停止后已启动或恢复健康心跳的历史事件不进入重要事件；趋势使用 5 分钟聚合；来源分布展示不同事件来源构成；事件列表支持事件类型、监控领域、资源名称和发生时间查询分页，不展示关联标识查询条件；主列表仅展示事件信息、当前状态、发生时间和操作入口四列，不展示事件内容；详情展示完整事件内容、关联 ID 和脱敏上下文，不展示完整问题、答案、文档片段或敏感凭证 | 通过（Chromium 自动化） |
| MON-UI-025 | 真实汇总 | 构造多页指标、任务、告警和事件数据 | 分别查询总览接口并切换列表分页 | 总览统计不随当前页变化，指标趋势不复用事件趋势，Worker 不进入任务明细，来源和优先级分布与全量授权范围一致 | 待执行 |
| MON-UI-026 | 空图处理 | 清空当前时间范围的指标、任务和事件数据 | 进入对应页面 | 页面展示空状态，不绘制 `[0]`、固定时间轴、零值折线或伪造健康结论 | 待执行 |
| MON-UI-027 | 查询样式 | 依次进入自主监控所有列表页面 | 对比平台管理查询区并执行查询、重置和刷新 | 查询区复用公共 QueryBar 和 Element Plus 控件，标签、控件、按钮及留白一致；标题与说明之间具有清晰纵向间距 | 待执行 |
| MON-UI-028 | 指标合同 | 已发布中文指标定义，分别构造有值、无样本、过期和失败结果 | 请求指标总览、分页和详情接口 | 总览关联中文指标名与监控领域；查询支持数据范围和时间窗口；详情返回口径、阈值、来源、趋势和关联告警；无样本不返回数值 0 | 通过（真实 HTTP + 后端自动化） |
| MON-UI-029 | 指标视觉 | 指标总览返回四类监控域及多种考核状态 | 对照 `tmp/image.png` 查看指标分析页面 | 结论面板、1.35:0.65 中部布局、四线趋势、独立图例、域分段状态条、完整查询栏和明细表结构与交互稿一致 | 通过（Chromium 自动化） |
| MON-UI-030 | 指标详情 | 指标明细存在真实结果或已发布定义 | 点击“查看详情”并切换有数据与无数据指标 | 抽屉展示编码、口径、阈值、样本、来源、计算状态、趋势和关联告警；无数据不绘制零值趋势，不出现演示数据 | 通过（Chromium 自动化） |
| MON-UI-050 | 详情范围 | 平台或租户指标存在结果，告警表使用资源编码保存指标范围 | 从指标列表打开详情 | 指标结果按 `scope_key` 查询，关联告警按 `resource_code` 和租户范围查询；接口不向告警表传递不存在的 `scope_key` 字段 | 通过（真实数据 + 后端自动化） |
| MON-UI-052 | 总览兼容 | 分别构造达标指标、预警指标、活动告警和全部事实为空四种真实接口响应 | 直接请求分析总览兼容接口 | 接口继续返回四态结构化结果，供 Agent 上下文和存量调用使用；前端不提供独立总览入口 | 通过（后端自动化） |
| MON-UI-053 | 入口合并 | 已授权用户拥有 `monitoring:analysis` 权限 | 进入 `/monitoring/analysis` 并检查页面入口 | 页面直接展示智能分析对话工作区；“分析总览”“分析对话”Tab 均不可见；工作区可正常加载和操作 | 通过（Chromium 自动化） |
| MON-UI-031 | 任务合同 | 构造索引任务、评测运行和 Worker 生命周期事件 | 请求任务总览、分页和详情接口 | 总览返回任务结论、状态分布、5 分钟趋势和 Worker 消费状态；分页支持任务名称、任务类型、当前状态、工作节点和创建时间；详情返回真实任务字段和脱敏执行证据 | 通过（真实 HTTP + 后端自动化） |
| MON-UI-032 | 任务视觉 | 任务总览返回积压、新增、完成、失败、超时数据和忙碌、延迟、空闲 Worker | 对照 `tmp/3f363cc616d0a0f5f7606e8b11eedc3c.png` 查看任务监控页面 | 页面展示任务结论、1.35:0.65 中部布局、积压阶梯线与四组吞吐柱、五个双字图例、紧凑 Worker 状态与汇总、完整查询栏和任务明细；标题说明保持上下间距 | 通过（Chromium 自动化） |
| MON-UI-033 | 任务边界 | 构造无任务且 Worker 空闲、无任务数据、任务接口失败 | 查看页面并打开任务详情 | 空闲 Worker 不标记异常；无任务不伪造统计或业务趋势，但状态趋势图仍保留坐标系、五项图例、固定高度和“当前时间范围暂无任务积压与吞吐数据”提示；接口失败展示错误状态；详情只读，不出现重试、暂停、删除和终止操作 | 通过（Chromium 自动化） |
| MON-UI-034 | 事件合同 | 构造任务、Worker、告警、采集与依赖事件，并包含关联 ID；另构造 Worker 停止后重新启动并恢复健康心跳 | 请求事件总览、分页和详情接口 | 总览返回重要事件、5 分钟趋势和四类中文来源分布；后续启动或健康心跳覆盖同一资源的历史停止状态，已恢复事件不进入 `focus_event`；分页支持事件类型、监控领域、资源名称和发生时间；详情只返回脱敏上下文 | 通过 |
| MON-UI-035 | 事件视觉 | 分别构造存在异常、仅有正常事件、完全无事件三种窗口 | 对照 `tmp/image.png` 和原型查看事件中心 | 重要事件与任务结论使用同款浅蓝背景、蓝色左边框、左侧标签、中间结论、右侧指标的紧凑横向样式；右侧数量或状态值位于上层，发生时间或最近事件时间位于下层，每个标签和值保持同一行；异常窗口展示详情入口；正常窗口展示“未发现异常事件”；空窗口展示“暂无事件”且不推断平台正常；页面保持 1.35:0.65 趋势与来源布局、来源进度条、统一查询栏和四列事件明细 | 通过（Chromium 自动化） |
| MON-UI-036 | 事件边界 | 构造普通 Worker 空轮询、敏感 payload、空数据和接口失败 | 查询列表并打开详情 | 普通 `worker_idle` 不作为独立事件；详情不展示问题、答案、文档内容和凭证；空数据不补假点；失败展示错误状态 | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-037 | 告警合同 | 构造触发中、已确认、已恢复和已关闭告警 | 请求告警总览、分页和详情接口 | 返回结论、生命周期、5 分钟趋势、三级优先分布和统计范围；分页支持级别、状态、监控领域、资源名称和发生时间；详情返回规则、证据和处理记录 | 通过（真实 HTTP + 后端自动化） |
| MON-UI-038 | 规则合同 | 构造多监控领域、级别、状态和版本规则 | 请求规则概览和分页，并执行新增、编辑和启停 | 概览返回总数、启用数和停用数；分页返回规则中文名称、领域、级别、触发条件、窗口、恢复条件、状态、版本和更新时间；编辑生成新版本，启停不删除历史告警 | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-039 | 通知合同 | 构造站内通知、Webhook、策略及成功、失败、重试记录 | 请求通知总览、渠道、策略和发送记录 | 总览返回渠道、策略、发送成功与失败统计；渠道与策略支持查找分页；发送记录支持渠道、状态、级别和时间查询并返回中文关联字段及脱敏失败信息 | 通过（真实 HTTP + Chromium 自动化） |
| MON-UI-040 | 告警视觉 | 三个 Tab 均存在真实数据 | 分别对照 `tmp/告警实例.png`、`tmp/规则配置.png` 和 `tmp/告警通知.png` | 使用线型 Element Plus Tabs；三个 Tab 分别实现截图中的总览层、查询区、表格或卡片、分页和单列抽屉表单，标题说明保持上下间距 | 通过（Chromium 自动化） |
| MON-UI-041 | 分析合同 | 分别构造未恢复告警、无告警但有指标事实、所有事实为空，并注入 Agent 超时 | 请求分析总览接口 | 有告警时返回可信度、影响分类、证据级别、时间线和人工建议；失败时保留原始告警和证据并返回分析暂不可用；无告警但有指标时返回正常分析态、指标证据和时间线；所有事实为空时才返回真实空态 | 通过（真实 HTTP + 后端自动化） |
| MON-UI-042 | 对话合同 | 以前端伪造证据创建会话并发送追问 | 查询会话及消息元数据 | 后端忽略伪造证据并绑定当前授权异常范围；回答标识自主监控 Agent，消息保存证据引用和执行状态 | 通过（后端自动化） |
| MON-UI-043 | 审计合同 | 构造不同人员、动作、目标、租户、时间和结果审计 | 请求选项、分页和详情接口 | 所有筛选在后端执行；列表返回中文人员、动作、真实资源名称和结果，不展示资源标识列；未记录资源名称时返回空值并展示“暂无”；详情返回请求标识、脱敏摘要和操作说明；租户范围不越权 | 通过（真实 HTTP + 后端自动化） |
| MON-UI-044 | 分析审计视觉 | 分析存在结构化异常，审计存在真实记录 | 打开参考证据并操作筛选按钮 | 查询区默认隐藏；点击“筛选”后展开两行平铺筛选面板，标题操作改为相邻的“收起”“重置”，面板内部无操作按钮；只显示证据类型、证据等级、“全部”和固定四字选项；点击选项立即筛选，无下拉框和查询按钮；选中项仅文字高亮，无边框和底色；每次重新打开抽屉恢复隐藏；事件和任务统一显示“运行事件”“任务事实”；明细固定为三列 | 通过（Chromium 自动化） |
| MON-UI-045 | 对话高度 | 使用不同桌面视口高度进入智能分析 | 调整浏览器高度并发送多轮消息 | 双栏问答区域自动填满当前可视区域；输入区保持可见；消息列表在区域内独立滚动，页面不出现由固定高度造成的大块底部空白 | 通过（Chromium 自动化） |
| MON-UI-046 | 问题输入 | 已进入分析对话并具有发送权限 | 在多行输入框中录入问题，分别使用 Enter、Shift+Enter 和“发送问题”按钮 | 输入区展示“输入问题，开始分析”、快捷键提示和右下角发送按钮；Enter 发送，Shift+Enter 换行；发送过程中显示加载状态且输入区保持可见 | 通过（Chromium 自动化） |
| MON-UI-047 | 时间展示 | 历史助手消息包含本轮 Agent 解析的自然语言时间和授权范围 | 打开分析对话并切换包含“昨天”问题的会话 | 会话标题下展示授权范围、昨天标签及实际起止时间，不使用固定“最近1小时”覆盖消息元数据 | 通过（Chromium 自动化） |

| MON-UI-049 | 问答复用 | 知识库问答与分析对话均可访问 | 检查两个页面的组件引用和浏览器 DOM，分别新建会话、切换历史、发送问题并观察加载状态 | 两个模块共同使用 `src/components/chat-workspace/comp.vue`，页面不重复实现会话列表、消息流和输入区；知识库参考资料正常展示，分析对话的异常范围、快捷问题和每轮证据入口正常展示 | 待执行 |
| MON-UI-050 | 会话操作 | 已存在至少两个监控分析会话 | 使用快速查找筛选会话；打开会话操作菜单，分别执行重命名、取消删除、确认删除和删除当前会话 | 快速查找按服务端结果刷新；菜单展示重命名和删除；重命名成功后列表即时更新，空名称和超长名称被拒绝；取消删除不改变数据；确认删除后会话不再出现且写入审计；删除当前会话后自动切换且不串租户 | 通过（后端自动化 + Chromium 自动化） |
| MON-UI-051 | 问答视觉 | 分析对话存在欢迎提示、历史消息、快捷问题和回答证据入口 | 与知识库问答对照公共区域，并检查监控业务插槽 | 公共会话、消息和输入区域保持同一视觉基线；正文为 14px，会话、范围、状态、快捷问题、证据入口和输入提示不低于 13px；欢迎卡、消息卡、快捷问题和证据入口对齐工整，无局部小字或样式冲突 | 通过（Chromium 自动化） |
| MON-MENU-001 | 菜单收口 | 重复执行菜单配置脚本 | 检查平台超级管理员和租户管理员的自主监控菜单授权 | 保留事件中心、审计管理菜单定义及页面能力，但删除两个角色与这两个菜单的关联；后续重复执行脚本不会重新授权 | 通过（脚本自动化） |
| MON-METRIC-001 | 规则发布 | 数据库已发布16个指标定义 | 重复执行正式 DML 并查询指标规则 | 每个指标存在一条启用的默认规则，规则包含高值异常、低值异常或信息型方向，重复执行不新增重复版本 | 通过（数据库实测） |
| MON-METRIC-002 | 范围聚合 | 同一窗口存在两个租户的问答事件 | 执行一次指标聚合 | 同时生成一份平台汇总和两份租户结果；平台查询不混入租户行，租户查询不读取其他租户 | 通过（后端自动化） |
| MON-METRIC-003 | 时间汇总 | 最近一小时前段有有效值、最新五分钟为空 | 查询最近一小时总览和列表 | 有效值不被最新空窗口覆盖；比例按累计分子分母计算，计数累计，数据状态为部分数据 | 通过（后端自动化＋数据库实测） |
| MON-METRIC-004 | 终态去重 | 同一失败问答先后产生失败和降级终态 | 执行窗口聚合 | 同一请求只计一次；新链路只产生一个最终终态，历史相邻终态回填时受控去重 | 通过（后端自动化＋数据库实测） |
| MON-METRIC-005 | 历史回填 | 已有窗口因历史规则缺失被写为无法判定 | 发布规则并执行最近一小时回填 | 有值窗口按新规则重算，平台及租户结果补齐；回填不创建历史告警或发送通知 | 通过（数据库实测） |

## 3. 安全与边界测试用例

| 编号 | 场景 | 操作步骤 | 预期结果 | 验收状态 |
| --- | --- | --- | --- | --- |
| MON-AGENT-013 | 敏感内容脱敏 | 查询问答遥测和评测事件 | 默认不展示完整问题、答案、文档片段、Token 或 API Key；需要查看时按权限加载 | 待执行 |
| MON-AGENT-014 | 租户隔离 | 使用租户管理员查询监控和评测详情并执行告警操作 | 只能查看和操作当前租户范围，不能通过 ID、Trace ID 或探针参数越权 | 待执行 |
| MON-AGENT-015 | 禁止自动重跑 | 模拟评测失败并触发监控告警 | 观察任务状态和调用记录 | 监控只生成告警和建议，不自动创建、重试、暂停或终止评测 | 待执行 |
| MON-AGENT-016 | 禁止循环调用 | 模拟监控异常、评测异常和问答异常同时发生 | 查看 Agent 调用链 | 不出现“评测等待监控、监控等待评测”的同步循环，不发生无限重试 | 待执行 |
| MON-AGENT-017 | 私有边界 | 检查跨 Agent 导入和工具注册 | 验证监控只能调用公开结构化协议 | 不导入问答或评测 Agent 的私有 Prompt、函数、模型实例、Runtime 或内部状态 | 待执行 |

## 4. 验收标准

- 问答智能体和自主评测智能体均能向监控提供可追踪的结构化运行事实。
- 监控探针与真实业务问答、评测任务和租户用量严格隔离。
- 评测质量结论、评测执行状态和基础设施健康状态分别表达，不相互覆盖。
- 监控异常不会自动修改、重试或终止问答、评测和知识库业务数据。
- 监控不可用时，问答和评测仍可按各自协议独立运行。
- 所有跨 Agent 调用均经过公开协议、权限校验、数据范围校验和资源预算校验。

## 5. 本次执行

2026-07-31 完成数据采集页面与左侧导航回归：

- `npx playwright test tests/e2e/monitoring.spec.ts`：`7 passed`，其中采集总览用例覆盖运行结论、状态趋势、执行结果、五域热力图、目标明细和侧栏选中样式。
- `npx playwright test tests/e2e/monitoring-live.spec.ts`：`2 passed`，不拦截前端接口，真实验证监控总览和数据采集两个页面；临时认证会话执行后已撤销。
- 真实采集总览返回 22 个目标、13 个 5 分钟趋势点、65 个五域热力格，目标明细查询和分页接口返回 HTTP 200。
- 自主监控列表控件统一后，Chromium 回归为 `8 passed`；数据采集、指标分析、任务监控、告警中心、事件中心和审计管理均验证存在 Element Plus 表单、表格和分页控件。
- 新增分页查询接口使用真实数据库和可撤销临时认证会话执行 HTTP 联调：指标、任务、告警、事件、审计、规则、渠道和策略 8 个分页接口均返回 HTTP 200，临时认证会话已撤销。
- 后端全量回归为 `155 passed`，前端格式检查、类型检查和生产构建均通过。
- 本轮补齐指标、任务、事件、告警专用汇总后，后端全量回归为 `156 passed`；8 个 overview/page 真实接口均返回 HTTP 200，临时认证会话已撤销。
- 前端 Prettier、类型检查、生产构建通过，自主监控 Chromium 回归为 `8 passed`；空趋势不再补 `[0]`，列表查询区统一复用平台管理公共 QueryBar。
- 任务监控查询栏移除页面私有网格覆盖，统一复用公共 Element Plus QueryBar；自动化验证五个查询项宽度均为 `325px`，标签和控件保持统一对齐。
- 事件中心完成真实接口重构：后端全量回归 `162 passed`，前端格式、类型和生产构建通过，Chromium 合同、空态和异常态回归 `13 passed`；真实前端代理调用事件总览、分页和详情接口回归 `1 passed`，临时认证会话已退出。
