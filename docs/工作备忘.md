# 工作备忘

## 2026-07-26 工作记录

### 明日继续事项

问答配置与自主评测目前仍存在流程串联问题，单模块功能可以操作，但“配置变更 → 评测任务创建/执行 → 结果查看 → 优化复测”的整体链路还不顺畅，暂不作为完整联调完成。

明日重点处理：

- 梳理问答配置发布、向量模型生效、索引完成与自主评测读取配置之间的状态关系。
- 验证自主评测创建、执行、结果、优化、复测流程是否使用同一知识库和有效配置版本。
- 检查前后端任务 ID、运行 ID、配置版本、索引版本的传递和回填。
- 补齐流程中断、轮询、空数据、失败重试和结果为空等异常场景。
- 完成端到端串联验证后，再更新测试用例和完成情况报告。

## 近期工作事项

日期：2026-07-25

1. 前端操作权限预埋测试。
2. 知识库问答调试。
3. 两个新增自研项目的开发学习。

日期：2026-07-20

## 明日继续事项

继续完善不同角色登录后进入系统的展示内容，重点确认：

- 平台超级管理员：平台管理、知识库管理及租户资源切换入口。
- 租户所有者、租户管理员：租户范围内的用户、组织、知识库和文档管理入口。
- 租户普通成员：按授权展示知识库、文档和问答能力，隐藏管理操作。
- 租户访客：进入独立访客问答工作区，仅展示授权数据源、会话、问答和参考资料。
- 组织管理员、组织成员：根据组织权限展示成员管理和组织授权相关入口。

## 需要重点检查

- 登录后路由是否与角色匹配。
- 左侧菜单是否只展示当前角色有权限的内容。
- 不同角色进入同一页面时，操作按钮和数据范围是否正确。
- 平台管理员、租户成员和访客之间是否存在错误跳转或越权展示。
- 原型设计、前端页面和后端接口权限是否保持一致。

## 今日已完成基础

- 完成访客问答工作区及授权数据源抽屉。
- 完成访客数据源远程搜索和分页接口。
- 完成访客问答、会话管理后端接口。
- 抽取并复用了问答工作区主体组件。
- 完成 `guest / guest` 访客测试账号和授权测试数据。
